import json

def lambda_handler(event, context):
    # TODO implement
    print(event)
    print("---")
    print(context)
    return {
        'statusCode': 200,
        'body': json.dumps(event['key1'])
    }
